# pytorch-cyclegan_1dcnn
# 运行train_cycleW.py得到损失数据（errors.mat）、预测结果（pred_res.mat）、模型参数（models.pth）
# 该代码是无监督双域风格迁移的通用框架，可应用于：
# 语音信号：如将男性语音转换为女性语音（保持内容不变，改变风格）。
# 时序数据：如将某设备的传感器数据转换为另一设备的格式。
# 特征迁移：如将图像的某类特征（如素描）转换为另一类特征（如彩图）。
